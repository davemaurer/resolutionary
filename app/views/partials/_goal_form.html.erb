<br>
<div class="large-6 large-centered columns">
  <%= form_for([@resolution, @goal]) do |f| %>
    <%= f.label "What is the goal you will use to fulfill your resolution", class: "edit-label" %>
    <%= f.text_field :name %>
    <%= f.submit "Create", class: "button round medium" %>
    <% end %>
</div>
